/**
 * vault_addPeer: Add a new peer connection to the vault.
 * vault_switchPeer: Switch the vault's active peer connection.
 */
export type FabricWalletMethods = "vault_addPeer" | "vault_switchPeer"
